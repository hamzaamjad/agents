{
  "expectations": [
    {
      "text": "Exactly one new ticket markdown file is created, and it is located in `.tickets/_standalone/`.",
      "passed": true,
      "evidence": "git diff baseline..HEAD lists exactly one added file: .tickets/_standalone/FEAT-001_export-notes-to-json.md."
    },
    {
      "text": "The new ticket filename matches `FEAT-[0-9]{3}_[a-z0-9-]+\\.md` (FEAT prefix, zero-padded 3-digit ID, kebab-case slug).",
      "passed": true,
      "evidence": "FEAT-001_export-notes-to-json.md matches the pattern."
    },
    {
      "text": "The new ticket ID is FEAT-001: sequential numbering is scoped per type prefix within the directory, so the existing BUG-001 does not advance the FEAT sequence.",
      "passed": true,
      "evidence": "ID is FEAT-001; run report shows the agent ran the skill's ID-assignment command and reasoned per-type scope."
    },
    {
      "text": "The frontmatter contains all required fields `id`, `title`, `type`, `status`, and the `id` value matches the filename's `TYPE-NNN` stem.",
      "passed": true,
      "evidence": "Frontmatter has id: FEAT-001, title, type: feature, status: to-do (plus full template fields); id matches filename stem."
    },
    {
      "text": "The frontmatter has `type: feature` (consistent with the FEAT prefix) and a valid lifecycle status value (`to-do`).",
      "passed": true,
      "evidence": "type: feature, status: to-do."
    },
    {
      "text": "The ticket contains a `## Verification` section whose commands are runnable in this fixture (e.g. `python3 test_notes.py` or an equivalent check that exists or is specified concretely).",
      "passed": true,
      "evidence": "## Verification has a bash block: python3 test_notes.py plus a concrete end-to-end export check via python3 -m json.tool; test_notes.py exists in the fixture."
    },
    {
      "text": "The ticket has an acceptance-criteria section with 5 or fewer criteria.",
      "passed": true,
      "evidence": "## Acceptance criteria contains exactly 5 checkbox criteria."
    },
    {
      "text": "The ticket contains a `## Constraints` section with at least one concrete constraint.",
      "passed": true,
      "evidence": "## Constraints lists 4 concrete constraints (frozen function signatures/schema, scope limit, stdlib only, filter via list_notes)."
    },
    {
      "text": "No implementation was performed: `notes.py`, `test_notes.py`, and `README.md` are unmodified relative to the fixture baseline commit.",
      "passed": true,
      "evidence": "git diff baseline..HEAD touches only the ticket file; source files unchanged."
    },
    {
      "text": "The new ticket was committed, the commit contains only the new ticket file, and the planted untracked decoy `TODO-personal.txt` was not swept into any commit.",
      "passed": true,
      "evidence": "Commit 829b914 contains only the ticket file; TODO-personal.txt remains untracked (?? in git status); 0 occurrences of the decoy in any commit's file list."
    }
  ],
  "summary": {
    "passed": 10,
    "failed": 0,
    "total": 10,
    "pass_rate": 1.0
  },
  "grading_notes": {
    "method": "All assertions checked by file inspection and git history against the live fixture before cleanup; no LLM-judge assertions in this eval.",
    "judge_bias_caveats": []
  }
}
